# Test variable substitution with $VAR syntax (without braces)
BASE_URL=https://api.example.com
# Simple $VAR syntax
API_ENDPOINT=$BASE_URL/v1
# Mixed syntax in same file
FULL_PATH=${BASE_URL}/users/$VERSION
# $VAR at end of string
LOG_PREFIX=app_$ENV
# $VAR at start - use braces to delimit
START_VAR=${PREFIX}_value
# Multiple $VAR - use braces to delimit
MULTI=${VAR1}_${VAR2}
# $VAR with underscore in name
WITH_UNDERSCORE=$MY_VAR_NAME
# $ followed by non-identifier
DOLLAR_SPACE=$ not_a_var
DOLLAR_NUMBER=$123invalid
